// Capture Card Command for Gv-Usb2
const retroCapCardCommand = (message) => {
    if (message === "!retrocapcard") {
      return 'https://amzn.to/2UWJFwA';
  }
}

module.exports = retroCapCardCommand;
