// Links to the available FrankerFaceEmotes for my Channel
const frankerFaceEmotes = (message) => {
    if (message === "!frankerfaceemotes") {
      return 'https://www.frankerfacez.com/channel/epistemicpolymath';
  }
}

module.exports = frankerFaceEmotes;
