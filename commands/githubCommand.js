// Github Link for the Bot
const githubCommand = (message) => {
    if (message === "!github") {
      return 'https://github.com/EpistemicPolymath/PolymathicalBot';
  }
}

module.exports = githubCommand;
