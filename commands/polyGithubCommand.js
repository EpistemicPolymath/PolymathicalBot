// My personal GitHub Link
const polyGithubCommand = (message) => {
    if (message === "!polygithub") {
      return 'https://github.com/EpistemicPolymath';
  }
}

module.exports = polyGithubCommand;
